import m from'mithril';import{Cart}from'../models/cart.js';export const Header={view(){const c=Cart.getCount();return m('nav',{role:'navigation'},m('div.logo',m('span','🛍️'),m('a',{href:'#!/',oncreate:m.route.link},'Galaxy Store')),m('div',m('a',{href:'#!/',oncreate:m.route.link},'Products'),m('a',{href:'#!/cart',oncreate:m.route.link},['🛒 Cart ',c>0?m('span.cart-badge',c):null])))}};
